%div(ng-init="ratingRange = [0.5, 1.5, 2.5, 3.5, 4.5]")
  %span.glyphicon.glyphicon-star(ng-repeat="val in ratingRange" ng-if="application.rating > val")>
  %span.glyphicon.glyphicon-star-empty(ng-repeat="val in ratingRange" ng-if="application.rating < val")
